#ifndef __PILA_H__
#define __PILA_H__

#include<stdio.h>
#include<stdlib.h>

struct Pila
{
  int dato;
  struct Pila *siguiente;
};

int agregar (struct Pila **pila, int valor);
int eliminar (struct Pila **pila, int valor);
void mostrar (struct Pila *pila);

#endif
